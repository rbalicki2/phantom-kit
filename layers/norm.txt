NORMAL - Layer Selector
j → Ins (typing mode)
n → Nav layer
m → Mouse layer, Shift+M → Label Mode (Homerow)
i → Layer I (window mgmt)
, → Comma layer
l → L layer
u → Term layer
h → Chrome/VSCode/TMUX (app-specific)
right_ctrl → send escape
