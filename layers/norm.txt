NORMAL - Layer Selector
j → Ins (typing mode)
n → Nav layer
m → Label Mode (Homerow), Shift+M → Mouse layer
i → Layer I (window mgmt)
, → Comma layer
l → L layer
u → Term layer
h → Chrome/VSCode/TMUX (app-specific)
right_ctrl → send escape
