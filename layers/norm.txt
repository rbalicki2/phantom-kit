NORMAL - Layer Selector
j → Ins (typing mode)
n → Nav layer
m → Label Mode, Fn+m → Grid Mode
i → Admin layer
, → Comma layer
l → L layer
u → Term layer
h → Chrome/VSCode/TMUX (app-specific)
right_ctrl → send escape
