NORMAL - Layer Selector
t → Ins (typing mode)
h → Nav layer
s → Label Mode, Fn → Grid Mode
c → Admin layer
n → Comma layer
a → L layer
i → Term layer
o → Chrome/VSCode/TMUX (app-specific)
' → AltIns + Escape
right_ctrl → send escape
